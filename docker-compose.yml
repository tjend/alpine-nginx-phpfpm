services:
  app:
    build:
      args:
        - TARGETARCH=amd64
      context: .
    image: localhost/alpine-nginx-phpfpm:latest
    ports:
      - 8080:80
    volumes:
      - ./php:/var/www
    working_dir: /var/www
